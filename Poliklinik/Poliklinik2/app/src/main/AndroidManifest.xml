<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poliklinik">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/klinik"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.Data_Pasien"></activity>
        <activity android:name=".Admin.Input_Dokter" />
        <activity android:name=".User.Input_Pasien" />
        <activity android:name=".Admin.Data_dokter" />
        <activity
            android:name=".Admin.Halamanutama_Admin"
            android:label="@string/title_activity_halaman_utama_admin" />
        <activity android:name=".Daftar_user" />
        <activity android:name=".User.Listdokter" />
        <activity android:name=".User.Halamanutama_User" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>